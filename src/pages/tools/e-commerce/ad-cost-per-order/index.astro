---
import BaseLayout from "../../../../components/layout/BaseLayout.astro";
---

<BaseLayout
  title="Ad Cost per Order Calculator — Free & Instant | SimpleTools"
  description="Calculate the maximum ad spend per order you can afford while hitting your target margin."
>
  <!-- ===================== -->
  <!-- Header / Intro -->
  <!-- ===================== -->
  <section class="calcHeader">
    <span class="badge">Free • No login • Instant</span>
    <h1>Ad Cost per Order Calculator</h1>
    <p>
      Calculate the maximum ad cost per order you can afford
      while still achieving your target profit margin.
    </p>
  </section>

  <!-- ===================== -->
  <!-- Calculator Body -->
  <!-- ===================== -->
  <section class="calcBody">
    <!-- Left : Inputs -->
    <div class="calcInputs">
      <div class="inputGroup">
        <label>Selling price (USD)</label>
        <input id="cpo_price" type="number" placeholder="e.g. 50" />
      </div>

      <div class="inputGroup">
        <label>Product cost (COGS)</label>
        <input id="cpo_cogs" type="number" placeholder="e.g. 15" />
      </div>

      <div class="inputGroup">
        <label>Target margin (%)</label>
        <input id="cpo_margin" type="number" placeholder="e.g. 30" />
      </div>

      <div class="inputGroup">
        <label>Platform fee (%)</label>
        <input id="cpo_platform" type="number" placeholder="e.g. 15" />
      </div>

      <div class="inputGroup">
        <label>Payment fee (%)</label>
        <input id="cpo_payment" type="number" placeholder="e.g. 2.9" />
      </div>

      <div class="inputGroup">
        <label>Shipping cost</label>
        <input id="cpo_shipping" type="number" placeholder="e.g. 5" />
      </div>
    </div>

    <!-- Right : Results -->
    <div class="calcResults">
      <div class="resultCard">
        <div class="resultItem">
          <span>Max Ad Cost per Order</span>
          <strong id="cpo_result">$0.00</strong>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== -->
  <!-- Content / SEO -->
  <!-- ===================== -->
  <section class="content">
    <h2>How it works</h2>
    <p>
      This calculator determines how much you can spend on advertising per order
      while still meeting your target profit margin.
    </p>

    <h2>Formula</h2>
    <pre>
Max Ad Cost =
Price
− Product cost
− (Platform fee % × Price)
− (Payment fee % × Price)
− Shipping
− (Target margin % × Price)
    </pre>

    <h2>FAQ</h2>
    <p><strong>What if the result is negative?</strong><br />
      A negative result means your current price or margin target is too low to run ads profitably.
    </p>

    <p><strong>Is this calculator free?</strong><br />
      Yes. No login or payment is required.
    </p>
  </section>

  <!-- ===================== -->
  <!-- Logic -->
  <!-- ===================== -->
  <script is:inline>
    (function () {
      const $ = (id) => document.getElementById(id);

      const price = $("cpo_price");
      const cogs = $("cpo_cogs");
      const margin = $("cpo_margin");
      const platform = $("cpo_platform");
      const payment = $("cpo_payment");
      const shipping = $("cpo_shipping");
      const result = $("cpo_result");

      function num(v) {
        const n = Number(v);
        return Number.isFinite(n) ? n : 0;
      }

      function calc() {
        const p = num(price.value);
        const c = num(cogs.value);
        const m = num(margin.value) / 100;
        const pf = num(platform.value) / 100;
        const pay = num(payment.value) / 100;
        const s = num(shipping.value);

        const targetProfit = p * m;
        const platformFee = p * pf;
        const paymentFee = p * pay;

        const maxAdCost =
          p - c - platformFee - paymentFee - s - targetProfit;

        result.textContent = maxAdCost.toLocaleString("en-US", {
          style: "currency",
          currency: "USD",
        });
      }

      [price, cogs, margin, platform, payment, shipping].forEach((el) => {
        el.addEventListener("input", calc);
        el.addEventListener("change", calc);
      });

      calc();
    })();
  </script>

  <!-- ===================== -->
  <!-- Styles (Template fixed) -->
  <!-- ===================== -->
  <style>
    /* 템플릿 스타일은 이전 계산기와 100% 동일 */
    .calcHeader {
      max-width: 1100px;
      margin: 0 auto;
      padding: 48px 20px 32px;
    }

    .badge {
      display: inline-block;
      font-size: 12px;
      padding: 6px 12px;
      border-radius: 999px;
      background: #eef4ff;
      color: #245bdb;
    }

    .calcHeader h1 {
      font-size: 32px;
      margin: 12px 0;
    }

    .calcHeader p {
      color: #555;
      max-width: 560px;
    }

    .calcBody {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 20px 60px;
      display: grid;
      grid-template-columns: 1.4fr 1fr;
      gap: 32px;
    }

    @media (max-width: 900px) {
      .calcBody {
        grid-template-columns: 1fr;
      }
    }

    .calcInputs {
      background: #fff;
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.04);
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .inputGroup {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    label {
      font-size: 14px;
      font-weight: 500;
    }

    input {
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid #ddd;
      font-size: 15px;
    }

    .calcResults {
      align-self: flex-start;
    }

    .resultCard {
      background: #fff;
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.04);
    }

    .resultItem span {
      font-size: 14px;
      color: #666;
    }

    .resultItem strong {
      font-size: 32px;
      font-weight: 600;
      display: block;
      margin-top: 4px;
    }

    .content {
      max-width: 800px;
      margin: 0 auto;
      padding: 40px 20px 80px;
    }

    .content h2 {
      margin-top: 32px;
    }

    pre {
      background: #f3f4f6;
      padding: 20px;
      border-radius: 12px;
      font-size: 14px;
      overflow-x: auto;
      margin-top: 12px;
    }
  </style>
</BaseLayout>
